# Override file for local development
# This file allows you to customize the docker-compose.yml for your local environment.
# Copy this file, remove the ".example" suffix, and uncomment/set the values you need.

services: {}

# Example overrides:
# services:
#   api:
#     environment:
#       # Override storage provider if you want to use S3 instead of MinIO
#       # STORAGE_PROVIDER: s3
#       # STORAGE_ENDPOINT: https://s3.amazonaws.com
#       # STORAGE_REGION: us-east-1
#       # STORAGE_ACCESS_KEY: your-aws-access-key
#       # STORAGE_SECRET_KEY: your-aws-secret-key
#       # STORAGE_FORCE_PATH_STYLE: false
#       # STORAGE_USE_SSL: true
#
#       # Or use GCS (native @google-cloud/storage client)
#       # STORAGE_PROVIDER: gcs
#       # GCS_PROJECT_ID: your-gcp-project-id
#       # GCS_KEY_FILE: /run/secrets/gcs-key.json  # mount the key file as a volume/secret
#       # GCS_CLIENT_EMAIL: your-service-account@your-project.iam.gserviceaccount.com
#       # GCS_PRIVATE_KEY: "-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----\n"
#
#       # Override other services URLs if running locally
#       # KEYCLOAK_AUTH_URL: http://host.docker.internal:8080
#       # ACL_APD_API_URL: http://host.docker.internal:8081
#       # CAT_API_URL: http://host.docker.internal:8082
#       # RABBITMQ_HOST: host.docker.internal
#
#     # Mount additional volumes for development
#     # volumes:
#     #   - .:/app
#     #   - /app/node_modules
#     #   - ~/.aws:/root/.aws:ro  # Mount AWS credentials if using S3
#     #   - ./gcs-key.json:/run/secrets/gcs-key.json:ro  # Mount a GCS service account key if using GCS
